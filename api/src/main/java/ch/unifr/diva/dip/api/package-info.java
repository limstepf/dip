/**
 * The DIP API is loaded by the host application's class loader and from there
 * published to the OSGi framework, such that host and bundles all share the
 * same classes.
 */
package ch.unifr.diva.dip.api;
