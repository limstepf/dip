/**
 * DIP parameters. Processors can have parameters which are defined in this package.
 * Such a parameter has to work with and without GUI/view, and the parameter (or value)
 * itself needs to be published as {@code ReadOnlyObjectProperty} s.t. we can listen
 * for modifications.
 */
package ch.unifr.diva.dip.api.parameters;
