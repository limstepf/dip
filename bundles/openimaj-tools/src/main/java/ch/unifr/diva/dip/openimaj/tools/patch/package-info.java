/**
 * Patched OpenIMAJ classes. Bugfixes and the like. Make sure to submit a pull
 * request, so we can get rid of this stupid package again.
 */
package ch.unifr.diva.dip.openimaj.tools.patch;
